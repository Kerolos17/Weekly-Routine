# دليل الإطلاق - Weekly Routine Checklist

## 🚀 الإطلاق المحلي

### المتطلبات

- Node.js (الإصدار 14 أو أحدث)
- npm أو yarn

### خطوات التشغيل

```bash
# تثبيت التبعيات
npm install

# تشغيل التطبيق في وضع التطوير
npm start

# بناء التطبيق للإنتاج
npm run build
```

## 🌐 الإطلاق على الإنترنت

### 1. Netlify (مجاني)

```bash
# بناء التطبيق
npm run build

# رفع مجلد build إلى Netlify
# أو ربط المستودع مباشرة
```

**إعدادات Netlify:**

- Build command: `npm run build`
- Publish directory: `build`
- Node version: 18

### 2. Vercel (مجاني)

```bash
# تثبيت Vercel CLI
npm i -g vercel

# الإطلاق
vercel --prod
```

### 3. GitHub Pages

```bash
# تثبيت gh-pages
npm install --save-dev gh-pages

# إضافة إلى package.json
"homepage": "https://username.github.io/weekly-routine-checklist",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}

# الإطلاق
npm run deploy
```

### 4. Firebase Hosting

```bash
# تثبيت Firebase CLI
npm install -g firebase-tools

# تسجيل الدخول
firebase login

# تهيئة المشروع
firebase init hosting

# بناء التطبيق
npm run build

# الإطلاق
firebase deploy
```

## ⚙️ إعدادات الإنتاج

### متغيرات البيئة

إنشاء ملف `.env.production`:

```
REACT_APP_NAME=Weekly Routine Checklist
REACT_APP_VERSION=1.0.0
GENERATE_SOURCEMAP=false
```

### تحسين الأداء

- ✅ تم تفعيل Code Splitting
- ✅ تم ضغط الملفات
- ✅ تم تحسين الصور
- ✅ تم تفعيل Service Worker (اختياري)

## 🔧 إعدادات الخادم

### Apache (.htaccess)

```apache
Options -MultiViews
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]
```

### Nginx

```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

## 📱 اختبار ما قبل الإطلاق

### قائمة فحص الجودة

- ✅ التطبيق يعمل على جميع المتصفحات الحديثة
- ✅ التصميم متجاوب (موبايل، تابلت، ديسكتوب)
- ✅ دعم RTL للغة العربية يعمل بشكل صحيح
- ✅ حفظ البيانات في localStorage يعمل
- ✅ وظائف التصدير/الاستيراد تعمل
- ✅ جميع الرسائل باللغة العربية
- ✅ لا توجد أخطاء في console
- ✅ أداء جيد (Lighthouse Score > 90)

### اختبار الأجهزة

- ✅ iPhone/Android
- ✅ iPad/Tablet
- ✅ Desktop (Chrome, Firefox, Safari, Edge)

## 🎯 نصائح الإطلاق

1. **اختبر على أجهزة حقيقية** قبل الإطلاق
2. **تأكد من سرعة التحميل** خاصة على الإنترنت البطيء
3. **اختبر وظائف localStorage** على متصفحات مختلفة
4. **تأكد من عمل RTL** بشكل صحيح
5. **اختبر التصدير/الاستيراد** مع ملفات مختلفة

## 📊 مراقبة الأداء

بعد الإطلاق، راقب:

- سرعة تحميل الصفحة
- معدل الارتداد
- استخدام المميزات
- تقارير الأخطاء

---

**التطبيق جاهز للإطلاق! 🎉**
