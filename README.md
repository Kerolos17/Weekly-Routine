# Weekly Routine Checklist - قائمة المهام الأسبوعية

تطبيق ويب مرن وواقعي لتتبع العادات والأهداف الأسبوعية باللغة العربية.

## 🌟 المميزات الأساسية

- **تتبع العادات متعدد الفئات**: الروحيات، التمرين، البرمجة، ممارسة الإنجليزية، والعادات الأساسية
- **إدارة المهام**: مهام إضافية مع تتبع الإنجاز
- **نظام المراجعة الأسبوعية**: تأمل في ما سار بشكل جيد، التحديات، والتعديلات للأسبوع القادم
- **تصور التقدم**: شريط تقدم يظهر نسبة الإنجاز في الوقت الفعلي
- **حفظ البيانات**: تخزين محلي للحفاظ على الحالة عبر الجلسات
- **تصدير/استيراد**: نسخ احتياطي واستعادة البيانات بصيغة JSON

## 🎯 الفلسفة التصميمية

- **مرن وواقعي**: بدون ضغط أو شعور بالفشل
- **واجهة ثنائية اللغة**: العربية أساسية مع بعض العناصر الإنجليزية
- **تتبع بصري للتقدم**: ملاحظات واضحة حول الإنجاز الأسبوعي
- **التركيز على التأمل**: التركيز على التعلم والتعديل بدلاً من الكمال

## 🚀 البدء السريع

### المتطلبات

- Node.js (الإصدار 14 أو أحدث)
- npm أو yarn

### التثبيت

```bash
# استنساخ المستودع
git clone [repository-url]
cd weekly-routine-checklist

# تثبيت التبعيات
npm install

# تشغيل خادم التطوير
npm start
```

سيفتح التطبيق في المتصفح على `http://localhost:3000`

### أوامر أخرى

```bash
# بناء للإنتاج
npm run build

# تشغيل الاختبارات
npm test

# إخراج التكوين (غير مستحسن)
npm run eject
```

## 🏗️ البنية التقنية

### التقنيات المستخدمة

- **React.js**: مكتبة واجهة المستخدم القائمة على المكونات
- **Tailwind CSS**: إطار عمل CSS مع PostCSS
- **Local Storage API**: حفظ البيانات من جانب العميل
- **Create React App**: نظام البناء

### بنية المشروع

```
src/
├── components/          # مكونات React
│   ├── Header.js       # التركيز الأسبوعي، التقدم، التصدير/الاستيراد
│   ├── HabitSection.js # أقسام تتبع العادات الفردية
│   ├── TaskManager.js  # إدارة المهام الإضافية
│   ├── WeeklyReview.js # مناطق نص التأمل
│   └── WeeklyRating.js # نظام تقييم الأسبوع
├── hooks/              # React hooks مخصصة
├── utils/              # وظائف مساعدة
├── data/               # بيانات التكوين
└── App.js              # المكون الرئيسي
```

## 📱 التصميم المتجاوب

التطبيق محسن لجميع أحجام الشاشات:

- **الهواتف الذكية**: تخطيط عمود واحد مع عناصر تحكم محسنة للمس
- **الأجهزة اللوحية**: تخطيط شبكي مع استغلال أفضل للمساحة
- **أجهزة سطح المكتب**: تخطيط متعدد الأعمدة مع جميع المميزات

## 🔧 التخصيص

### إضافة أقسام عادات جديدة

1. افتح `src/data/defaultSections.js`
2. أضف كائن جديد إلى مصفوفة `DEFAULT_SECTIONS`:

```javascript
{
  title: "🎨 الإبداع",
  items: [
    "رسم (30 دقيقة)",
    "كتابة يوميات",
    "تعلم مهارة جديدة"
  ],
  notes: ["ملاحظات إبداعية"]
}
```

## 🌐 دعم RTL

التطبيق يدعم بشكل كامل النص من اليمين إلى اليسار (RTL) للغة العربية:

- تخطيط RTL تلقائي
- خطوط محسنة للنص العربي
- عناصر واجهة مستخدم متكيفة مع الاتجاه

## 📊 إدارة البيانات

- **التخزين المحلي**: جميع البيانات محفوظة محلياً في المتصفح
- **التصدير**: حفظ البيانات كملف JSON
- **الاستيراد**: استعادة البيانات من ملف JSON
- **النسخ الاحتياطي التلقائي**: حفظ تلقائي عند كل تغيير

## 🔒 الخصوصية والأمان

- جميع البيانات محفوظة محلياً فقط
- لا يتم إرسال أي بيانات إلى خوادم خارجية
- تحكم كامل في بياناتك الشخصية

---

**مرن وواقعي - بناء عادات صحية بدون ضغط** ✨
